name: Hello world
on: 
  push:
    branches: 
      - main
  workflow_dispatch:

jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: echo command
        run: echo "Hello, world"
  
