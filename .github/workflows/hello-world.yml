name: Hello world
on: 
  push:
    branches: 
      - main
  workflow_dispatch:

jobs:
  Echo text:
    runs-on: ubuntu-latest
    steps:
      - name: echo command
        run: echo "Hello, world"
  
